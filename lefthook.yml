pre-commit:
  parallel: true
  jobs:
    - name: fmt
      run: cargo fmt -- --check

pre-push:
  parallel: true
  jobs:
    - name: test
      run: cargo test --locked
    - name: clippy
      run: cargo clippy --all-targets --locked
