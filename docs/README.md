# LoopW docs

- `user/` — how to use it. Start with `user/install.md`.
- `internals/` — why it is built this way. Start with
  `internals/architecture.md`.
- `operations/` — how to build, verify, and ship it. Start with
  `operations/development.md`.
- `ROADMAP.md`, `QA.md`, `BUGS.md`, `PERFORMANCE.md` — status logs and
  evidence. They record what ran and what remains, not how to use the app.
- `../specs/settings-ui-redesign-spec.md` — frozen settings requirements.
